apiVersion: v1
kind: Secret
metadata:
  # (a) bootstrap-token-<token id>
  name: bootstrap-token-4dd4f4
  namespace: kube-system
type: bootstrap.kubernetes.io/token
stringData:
  description: "The default bootstrap token generated by 'kubeadm init'." # (b) Token IDとSecret
  token-id: 4dd4f4
  token-secret: a27417dfd6f602bc
  # 有効期限
  expiration: 2019-10-15T02:29:41Z
  # Tokenを認証に利用できるかのフラグ usage-bootstrap-authentication: "true"
  # Tokenを署名に利用できるかのフラグ
  usage-bootstrap-signing: "true"
  # (c) 拡張Group
  auth-extra-groups: system:bootstrappers:worker,system:boot
